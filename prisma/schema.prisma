generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model urls {
  name String @unique @db.VarChar(20)
  url  String @db.VarChar(50000)
}
